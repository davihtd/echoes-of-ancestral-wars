export enum Layers {
  BACKGROUND = 0,
  CHARACTER = 1,
  FOREGROUND = 2,
  UI = 3,
}

export const Config = {
  SHOW_PLAYER_COLLISION: true,
  SHOW_COLLISIONS: true,
  SHOW_COLLISION_ZONES: true,
};
